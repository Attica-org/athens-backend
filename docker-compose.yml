services:
  backend:
    image: ${DOCKER_IMAGE}
    env_file: .env
    volumes:
      - ./log:/log
    restart: always
    depends_on:
      - redis
    networks:
      - ubuntu_app-network

  redis:
    image: redis:latest
    container_name: athens-redis
    ports:
      - 6379:6379
    volumes:
      - redis-data:/data
    restart: always
    networks:
      - ubuntu_app-network

networks:
  ubuntu_app-network:
    external: true

volumes:
  redis-data:
